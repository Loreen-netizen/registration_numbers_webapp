<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>registration_number_widget</title>
    {{!-- <link rel="stylesheet" href="./registration_number.css"> --}}
</head>

<body class="main">
    <div class="theMain">

        <div>


            {{#if messages.error}} <p class="alert alert-danger alert-dismissible fade show" role="alert">
                {{messages.error}} e.g CA 123-456 or CA 123</p>
            <button type="button" class="close" data-dismiss="alert" aria-label="Close">
                <span aria-hidden="true">&times;</span>
                {{/if}}

        </div>


        <div class="heading">
            <h1>Registration Number:</h1>
        </div>
        <form action="/reg_Numbers" method="POST">
            <div class="row my-5 ">

                <div class="">

                    <h4> <input type="text" value="" placeholder="Enter Reg Number e.g CA 123-456" id="regNumberInput"
                            name="theRegNumber" class="enterReg"></h4>
                </div>

                <div class=""> <button name="addRegNumber" type="submit"
                        class="btn btn-outline-default mb-3 ml-5">Add</button>
                </div>
            </div>
        </form>


        <form action="/selectTown" method="POST">

            <div class="row">


                <div class="">

                    <select name="theTown" class="dropdown mt-3 towns" type="submit">

                        <option value="">Filter By Town</option>

                        <option value="allTowns">All Towns</option>
                        {{#each towns}}
                        <option value="{{this.town_string}}">{{this.town_name}}</option>
                        {{/each}}

                    </select>
                </div>
                <div class="mt-3">
                    <button type="submit " class="btn btn-outline-default mb-3 ml-5" name="submitTown">Filter</button>
                </div>
            </div>

        </form>
        <br>
        <div class="liMainDiv">
     <div class="row">
            {{#each townSelected}}
            <ul name="ul" class="townsList">
               
                    <li>{{this.reg_number}}</li>
                
            </ul>

            {{/each}}


            {{#each data.allRegNumbers}}
            <ul name="ul" class="townsList">
                <div class="listDiv">
                    <li>{{this.reg_number}}</li>
                </div>
            </ul>

            {{/each}}

</div>

 </div>
 <br>
        <form action="/clearReg" method="POST">

            <div class="row">
                <div class="">

                    <button {{clearEntries}} id="clearButton" class="btn btn-outline-default mb-3 ml-5" name="clearRegNumber"
                        type="submit">Clear
                        Entries</button>


                </div>
            </div>
        </form>

    </div>
</body>


</html>